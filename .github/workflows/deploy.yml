name: Deploy

on:
  workflow_dispatch:
  push:
    branches: [main]
    tags: ['v*']

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Deploy pipeline placeholder. Configurar target (Railway/Render/AWS) en Fase 7."
